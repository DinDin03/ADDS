#ifndef FINDER_H
#define FINDER_H

#include <vector>
#include <string>

class Finder {
public:
    static std::vector<int> findSubstrings(const std::string& s1, const std::string& s2);
};

#endif // FINDER_H
